---
interface Props {
  className?: string
}

import cx from "classnames"
import styles from "./section.module.css"

const { className, ...rest } = Astro.props as Props
const mergedClassName = cx(styles.section, className)
---

<section class={mergedClassName} {...rest}>
  <slot />
</section>
