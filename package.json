{
  "name": "@infonomic/uikit-demo",
  "description": "",
  "main": "index.js",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "scripts": {
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "build": "turbo run build",
    "start": "turbo run start",
    "preview": "turbo run preview",
    "storybook": "turbo run storybook",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint --continue",
    "typecheck": "turbo run typecheck --continue",
    "test": "turbo run test",
    "version-packages": "changeset version",
    "release:tag": "turbo run build --filter=@infonomic/uikit && changeset tag",
    "release:npm": "turbo run build --filter=@infonomic/uikit && changeset publish"
  },
  "license": "ISC",
  "dependencies": {
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@changesets/cli": "^2.29.6"
  },
  "engines": {
    "node": "^18.20.2 || >=20.9.0"
  }
}
